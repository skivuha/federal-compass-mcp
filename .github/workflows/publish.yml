name: Publish to npm

on:
  workflow_dispatch:

jobs:
  publish:
    uses: ./.github/workflows/_publish.yml
    permissions:
      contents: read
      id-token: write
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
